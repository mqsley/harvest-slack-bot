specVersion: "v1beta"
serviceId: "harvest-slack-bot"
location: "us-west1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "harvest-slack-bot-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
